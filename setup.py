from setuptools import setup

setup(name='newstat',
      version='0.1',
      description='Data Mining project',
      author='Mikhail Mishin, Max Reuter',
      packages=[
          'netstat'
      ],
      )